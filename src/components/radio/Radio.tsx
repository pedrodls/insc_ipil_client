export default function Radio() {
    return (
        <>
            <div className="form-check d-flex justify-content-center">
                {/*<input className="form-check-input" value={'all'} checked={true} type="radio" name="flexRadioDefault" id={`flexRadioDefault1`} onChange={(e) => changeRadioButton(s.teacher.id, e)} disabled={disabledCheck} />*/}
            </div>
        </>
    )
}